# SDF.R V16.2.2 引継書

最終更新: 2026-09-11
対象ブランチ: `feature/multitree-phase0`（master からの分岐。V16.2.1 と同じブランチの続き）
前提バージョン: V16.2.1（**未リリース**。V16.2.2 はその上に乗っている）

V16.2.1 までの経緯は [HANDOVER_V16.2.1.md](HANDOVER_V16.2.1.md) にある。
このドキュメントは **V16.2.2 で足したぶんだけ**を、そこだけ読めば再開できるように書いてある。

---

## 0. 3行で

- 外部から「Mirror Blend と最終メッシュ側の Smoothness が連動していないのでは」と指摘を受けて周辺を測った。
- **指摘そのものは白**。代わりに**プレビューの Mirror Blend が最終メッシュと逆方向に効いていた**ので直した。Python とシェーダーのみで **Rust には触っていない**。
- **実機のUI目視がまだ**。Windows のパッケージは作ってあるが、Mac / Linux の zip は CI 未実行。**コミットもしていない**。

---

## 1. いま何が終わっていて、何が残っているか

| | 状態 |
|---|---|
| プレビュー修正（shader.py） | 完了・ヘッドレス検証済み |
| UI 警告追加（ui.py） | 完了・未目視 |
| V16.2.2 ツリー3種の作成 | 完了 |
| Windows パッケージ zip | 完了（後述の SHA あり） |
| CI ワークフロー追加 | 完了。push 済み・実行済み（run 34545919928 成功） |
| Mac / Linux の zip | 完了。CI から取得し検証済み |
| `dist_V16.2.2/` とチェックサム表 | 完了（3プラットフォーム揃った） |
| リリースノート / 告知 / UI Command Inventory の 16.2.2 版 | **未着手** |
| **実機のUI目視** | **未実施。ここが最優先** |
| **git コミット** | 完了。`94b4fd1..97a0b71` を push 済み |

### 再開したらまずこれ

2〜4 は 2026-09-11 に済ませた。残りは 1 と 5。

1. 4章の手順で Windows パッケージを入れて、5章の表を目視で潰す。**ここだけが残った最優先**。
2. ~~コミット~~ 済み（`94b4fd1..97a0b71`）。
3. ~~push して CI を回し、Mac / Linux の zip を取得~~ 済み。
4. ~~`dist_V16.2.2/` を作ってチェックサム表を書く~~ 済み。
5. リリースノート類の 16.2.2 版を書く。9章の「16.2.1 を飛ばすか」を先に決める必要がある。

---

## 2. 何を直したか

### 2.1 プレビューの Mirror Blend が「向き自体が逆」だった（本体）

旧実装（V16.2.1 の `shader.py`）は、ミラーの折り返しを丸める近似だった:

```glsl
lp.z = sqrt(lp.z * lp.z + mb * mb * 0.25) - offset;
```

これは外へ膨らむ最終メッシュとは逆に、**形を内側へ食い込ませる**。さらに
`mb > 2 * (offset + 形状サイズ)` で**プレビューから形が完全に消える**。
r=0.15 / offset=0.25 なら mb=0.8 で消滅し、実測と式が一致した。

折り返しは `abs()` に戻し、距離側に polynomial smin の補正項を近似で入れる方式へ変更した。
「反対側コピーとの距離差 ≒ 2*|ミラー軸のローカル座標|」と見なすと、1回評価のまま
`k*h*(1-h)` を再現できる（継ぎ目で k/4 と厳密解に一致、|座標| >= k/2 で 0 になり素の形に戻る）:

```glsl
float sdf_mirror_seam_cut(float c, float k){
    float h = clamp(0.5 + abs(c) / k, 0.0, 1.0);
    return k * h * (1.0 - h);
}
```

**軸ごとの補正は合計する。** 同じ距離を n 回 smin で重ねると原点で n*k/4 内側に寄るため
（2コピーで k/4、4で k/2、8で 3k/4）、`max` では多軸で明確に足りない
（3軸 blend=2.0 で 1.69 対 実測 2.99）。ここは一度 `max` で書いて実測で否定した箇所なので、
**将来 `max` に戻さないこと**。

最終メッシュとの幅の誤差（1軸、r=0.15 / offset=0.25）:

| blend | 旧プレビュー | 新プレビュー |
|---|---|---|
| 0.40 | 0.1100 | 0.0290 |
| 0.80 | 0.8562（消滅） | 0.1203 |
| 2.00 | 1.3625（消滅） | 0.0600 |

多軸を含めた最悪値で相対 17%、通常域（blend <= 1.0）は 1〜10%。
**`mirror_blend = 0` のときは補正 0 + `abs()` 折り返しで従来と完全に同一**なので、
既存ファイルの見た目は動かない。

### 2.2 UI 警告: Blend > 2 × Offset で形全体が太る

コピーが重なる/近いと、smooth union の性質で継ぎ目だけでなく**形全体**が膨らむ。
半径 0.15 の球を Z ミラー、blend=1.0 で測った x 幅（x はミラー軸ではないので、
本来 Mirror Blend で変わってはいけない方向）:

| offset | blend=0 | blend=1.0 |
|---|---|---|
| 0.00 | 0.3000 | 0.8000 |
| 0.25 | 0.3000 | 0.6240 |
| 0.50 | 0.3000 | 0.3103 |

境界は綺麗に **`mirror_blend > 2 * mirror_offset`** に出る。SDF の性質上避けられない
（直すなら継ぎ目局所化の設計変更）ので、`ui.py` で該当時に警告を出すに留めた。
**offset 0 のミラー（左右対称形を作る最も一般的な使い方）が最も派手に化ける**ので、
注意喚起の価値は高い。

### 2.3 触ったファイル

| ファイル | 内容 |
|---|---|
| `rust_gpu_sdf_addon/shader.py` | `sdf_mirror_seam_cut()` 追加、Mirror ブロック書き換え、`dd -= mirror_cut` |
| `rust_gpu_sdf_addon/ui.py` | Mirror Blend の警告ラベル 2 行 |

Windows / LINUX / MAC の3ツリーすべてに同内容で入れてある（md5 一致を確認済み）。
**Rust / WGSL は一切変更していない**ので、`.pyd` は 16.2.1 のものをそのまま使える。

---

## 3. 白だったもの（コードは触っていない）

指摘の裏取りで測ったが、実害が確認できなかったもの。**同じ疑いを持った人が再び調べ直さないよう**に残す。

- **AABB / bound_radius に `mirror_blend` が入っていない**
  `lib.rs` の `extra_pad` も `detect.wgsl` の `bound_radius` も、`smoothness` は足すのに
  `mirror_blend` を足していない。ブレンドで膨らんだ部分がブロックカリングで落ちる理屈は
  立つが、**スライダー上限 2.0 まで振っても再現しなかった**。Blend Profile 5種 × 1/2/3軸 ×
  MC/DC の全ケースで「ミラー」と「実体コピー + Smoothness」が頂点数まで一致し、
  ミラー側が小さく出た例が一つも無い（カリング落ちなら必ず小さくなる）。
  パディングを広げるコストだけが残るので**入れない**判断。理論上の穴としてここに残す。
- **Auto Domain**: `engine.py` の auto domain は `max_s * 1.5` と原点距離しか見ておらず
  mirror_offset も blend も smoothness も無視するが、offset 20（z幅 42）まで手動ドメインと
  完全一致した。切れない。
- **Soft プロファイルの極端な膨張**: `Mirror Blend + Soft` と `Smoothness + Soft` が
  完全に同じ数値になる。exp smin は `d1 == d2` で `d - k` を返す（2つの形がどれだけ離れていても
  全体が k 膨らむ）性質で、**Mirror Blend 固有ではなく既存挙動**。今回のスコープ外とした。
  ただし Mirror Blend の上限 2.0 と組み合わせると簡単に破綻域に入るので、いずれ別途扱う価値はある。
- **Mirror Blend の幅 k が Smoothness と独立なのは仕様**。プロファイルと Chamfer Smooth は
  プリミティブ側から借りており、CPU（`lib.rs` の `eval_primitive_mirrored`）と
  GPU（`common.wgsl` の `evaluate_shape_mirrored`）の両パスで一致している。
- **軸数ごとのコスト**: 1軸 1.71x / 2軸 2.93x / 3軸 4.13x（blend=0 比）。
  評価回数 2^軸数 の理論値より軽く、プロパティの説明どおり。

---

## 4. パッケージと版の切り出し

- ツリー: `Rust-GPU-SDF-V16.2.2` / `_LINUX` / `_MAC`
  16.2.1 から複製。`target/` と `__pycache__` と旧 zip は持ち込んでいない。
- 版の識別子を更新した箇所: `bl_info` の `(16, 2, 2)`、Loader Marker の print、
  GPU エンジンの初期化 / ウォームアップのログ、各 `build_sdf_addon.ps1` / `.sh` の zip 名。
  **過去の経緯コメント中の "V16.2.1:" は履歴なので触っていない**（触ると経緯が読めなくなる）。
- CI: `.github/workflows/build-sdf-r-v16-2-2-cross-platform.yml`（16.2.1 版からの複製）。
  **push で走る設定なので、コミットして push した時点で動く**。
- Windows パッケージ: `Rust-GPU-SDF-V16.2.2/SDF_R_16_2_2.zip`
  - サイズ 14,612,661 bytes
  - SHA-256 `1d9fb62742272e507a3f99e964992e2b7874dcf44840fcf630ae8f678c5a8f46`

  **2026-09-11 に作り直した。** 当初は cargo を回さず旧PCの `.pyd` を同梱していた
  （14,716,307 bytes / SHA-256 `9c7f9649...`）。PC 入れ替え後の新環境で
  `build_sdf_addon.ps1` を通す疎通確認を兼ねてフルビルドしたため、`.pyd` が
  6,410,240 → 6,189,568 bytes に変わっている。**Rust ソースは無変更で、差は
  ツールチェーン（この PC は cargo 1.98.1）由来**。旧 SHA を参照している箇所が
  あれば上の値に読み替えること。
  - Mac / Linux は CI（run 34545919928）の成果物。3プラットフォームぶんの
    チェックサム表は `dist_V16.2.2/CHECKSUMS.md` にある。

### 入れ方

1. Preferences → Add-ons で既存の SDF.R を**無効化 → 削除**
2. `Rust-GPU-SDF-V16.2.2\SDF_R_16_2_2.zip` をインストール
3. コンソールに `SDF.R: Loader Marker V16.2.2` が出ることを確認
   （`V16.2.1` が出たら旧版が残っている）

---

## 5. 実機で見ること（未実施）

**必ずコンソールを開いたまま操作すること。** パネルの `draw()` で出た例外は画面上ほとんど
無症状で、コンソールにしか出ない。

| # | 操作 | 合格基準 |
|---|---|---|
| 1 | Layout Mirror を有効化し、Mirror Blend を 0 → 2 へ動かす | プレビューが**膨らむ**（旧版は縮んで、上げ切ると消えた） |
| 2 | 同じ状態でメッシュを生成し、プレビューと見比べる | 両者が**同じ向きに**動く。これが今回の発端 |
| 3 | Offset を小さく / Blend を大きくする | `Blend over 2x Offset swells the whole shape` の警告が出て、実際に形全体が太る |
| 4 | Mirror Blend = 0 に戻す | **旧版と完全に同一**。ここが変わっていたら回帰 |
| 5 | 上を一通り操作する | `Writing to ID classes in this context is not allowed` が一度も出ない |

4 が回帰チェック、1〜3 が今回の修正の確認。
GLSL のコンパイル自体は `tests_V16.2.1/test_preview_shader_compiles.py` で確認済み。

---

## 6. テスト

新しく足したのは 9 本。すべて `tests_V16.2.1/` に置いてある
（16.2.2 用のディレクトリは切っていない。テストは版をまたいで使うため）。

| ファイル | 何を見るか |
|---|---|
| `test_mirror_blend_aabb.py` | blend スイープ。実体コピーとの差分と smin の解析解との照合 |
| `test_mirror_blend_aabb2.py` | Blend Profile 5種 × 1/2軸 × MC/DC の網羅 |
| `test_mirror_blend_edge_cases.py` | offset 別の膨張、1/2/3軸の一致、軸数ごとのコスト |
| `test_mirror_blend_preview_gap.py` | 旧プレビュー式と最終メッシュの乖離（消滅の再現） |
| `test_mirror_blend_preview_fix.py` | 新旧プレビュー式の誤差比較 |
| `test_mirror_blend_preview_axes.py` | 多軸での近似（合計 vs max）の誤差 |
| `test_soft_profile_inflation.py` | Soft の膨張が Mirror Blend 固有か否かの切り分け |
| `test_mirror_auto_domain.py` | Auto Domain がミラーで切れないか |
| `test_preview_shader_compiles.py` | GLSL が実際にコンパイルできるか |

### 注意

- プレビュー系のテストは **GLSL を回していない**。`shader.py` と同じ式を Python で再現して
  等値面を測っている。**`shader.py` の式を変えたらテスト側の式も直す必要がある**。
- `test_preview_shader_compiles.py` だけは GPU が要るので `-b`（バックグラウンド）では動かない。
  ウィンドウを一瞬起動して、タイマーでコンパイルだけ試して終了する作りにしてある。
- `test_register_cycle.py` は**引数に zip を渡す**（他の多くはツリーのディレクトリ）。間違えると
  PermissionError になる。

### 実行結果（2026-09-11）

配布 zip を展開したものに対して `test_native_loader` / `test_multitree` / `test_all_clear` /
`test_mirror_blend`、zip 自身に対して `test_register_cycle`、新ツリーに対して `test_regression`
が全て ALL PASS。

**2026-09-11 に PC 入れ替え後の環境で取り直した（Blender 5.2.1 / cargo 1.98.1）。**
4章のとおり Windows zip を作り直したため、上の結果は旧PCビルドに対するものだった。
作り直した配布物に対して同じ6本を流し直して**全て ALL PASS**。あわせて
`test_preview_shader_compiles` が実ウィンドウで `SHADER: COMPILED` / `BLIT: COMPILED`。

測定スクリプト8本（`test_mirror_blend_*` / `test_soft_profile_inflation` / `test_mirror_auto_domain`）
も完走した。**こちらは合否ゲートではない**ので、ログの「DIFF」「消滅」を不具合と読まないこと。
`test_mirror_blend_preview_gap` は旧式をハードコードした診断なので、修正後も
「プレビューは消滅」と出るのが正しい。`test_mirror_blend_aabb` の「差あり」は解析解との比較で、
mirror と reference 自体は dz=dx=0.0000 で一致している。

```
blender -b --factory-startup --python tests_V16.2.1/<test>.py -- <ツリーか zip のパス>
```

---

## 7. 判断の記録（なぜそうしたか）

- **AABB のパディングを直さなかった**: 実害が再現しなかったため。修正自体は 1 行で、
  境界を広げるだけなので既存の形は変わらない（片方向にしか動かない）。だが Mirror を
  使っていない全プリミティブの AABB まで膨らませればブロックあたりの評価数が増えて
  全ユーザーが遅くなる。ゲートを付けて足すことはできるが、**再現しない不具合に対して
  コストを払う理由がない**。再現例が出たら入れればいい。
- **プレビューを「両側評価」にしなかった**: 最終メッシュと厳密一致させるにはループを
  1形状1評価から作り替える必要があり、プレビューは毎フレーム走るのでコストが素直に倍になる。
  V16.2.1 で意図的に見送った判断であり、覆すなら別バージョンの仕事。近似の精度を上げる方向で足りた。
- **軸ごとの補正を合計にした**: 上記 2.1 のとおり実測で `max` を否定したため。
- **2.2 を SDF 側で直さなかった**: smooth union の数学的性質そのもので、
  継ぎ目局所化という設計変更になる。警告で伝える方が費用対効果が高い。
- **Rust を触らなかったので cargo を回さずに梱包した**: `.pyd` は 16.2.1 と同一。
  ビルド環境の差でバイナリが変わる余地を持ち込まないほうが安全。

---

## 8. 既知の落とし穴（この作業で踏んだもの）

- **対照実験で「実体コピー + Smoothness」を作るときは、全部のコピーに同じ Smoothness を与えること。**
  先頭だけ 0 にすると accum への最初の合流の k が変わり、Chamfer で形が変わる。
  これで一度「Chamfer と 2軸ミラーがズレている」と誤判定した。
- **プレビュー式を数値で測るときは走査範囲に注意。** 走査幅で頭打ちになった値を
  「近似が足りない」と読み違えかけた。blend を上げると形は大きく膨らむ。
- **プリミティブが数個だとブロックカリングが実質効かない。** カリング絡みを検証するときは
  遠方にダミーを撒く必要がある（`test_mirror_blend_aabb.py` は 120 個撒いている）。

---

## 9. 未リリースであることの再確認

V16.2.1 自体がまだリリースされていない。**V16.2.2 は 16.2.1 を置き換える形で出す**のか、
16.2.1 を出してから 16.2.2 を追うのかは決めていない。Rust が同一なので、
**16.2.1 を飛ばして 16.2.2 を出すのが素直**だと思われる。その場合:

- リリースノート / 告知 / UI Command Inventory は 16.2.2 として書き直す
- V16.2.1 で作りかけの `SDF_R_V16_2_1_*` 系ドキュメント（未コミットのものを含む）の扱いを決める
- `dist_V16.2.1/` を出すかどうかも合わせて決める

---

## 10. コミットの分け方（案）

まだ何もコミットしていない。以下で分けるつもりだった。

1. `shader.py` / `ui.py` の修正本体（3ツリーぶん）
2. テスト 8 本の追加
3. V16.2.2 ツリー3種と CI ワークフロー
4. 引継書（この文書と `HANDOVER_V16.2.1.md` の 10章）

`.gitignore` は `Rust-GPU-SDF-V*/` を除外しつつ `!Rust-GPU-SDF-V*_MAC/` と
`!Rust-GPU-SDF-V*_LINUX/` で戻している。つまり:

- **Windows ツリー（`Rust-GPU-SDF-V16.2.2/`）は ignore 対象**。パッケージ zip もここにあるので
  **リポジトリには入らない**。配布物は `dist_V16.2.2/` に置く運用（16.2.1 と同じ）。
- **`_LINUX` / `_MAC` は追跡対象**。`git add` でそのまま入る（`-f` は不要）。
  CI がこの 2 つを見てビルドするので、**この 2 つが入っていないと Mac / Linux の zip は作れない**。
