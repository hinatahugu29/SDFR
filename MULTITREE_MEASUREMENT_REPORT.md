# 複数ツリー方針 実測レポート

計測日: 2026-09-08
環境: Windows 11 / RTX 3060 (Vulkan) / Blender 5.1.1 `--background` / V16.2.0 の `rust_gpu_sdf.pyd`
条件: 特記なき場合 Marching Cubes、weld なし、対称なし。各点は warm-up 後の中央値（3-5回）

---

## 1. SDF 評価コストの実測

### 1-1. リクエスト1回の下限コスト

| 解像度 | 1プリミティブ |
|---|---|
| 32 | 2.40 ms |
| 48 | 3.03 ms |
| 64 | 2.66 ms |

**ツリーを1本増やしたときの固定費は約 3 ms**（ただし後述の通り、変更が無いツリーは
ハッシュゲートで評価自体が走らない）。

### 1-2. 解像度スケーリング（8プリミティブ / domain 10）

| res | 32 | 48 | 64 | 96 | 128 | 192 | 256 |
|---|---|---|---|---|---|---|---|
| ms | 14.6 | 17.7 | 21.6 | 28.4 | 43.8 | 66.5 | 101.7 |

res を 8 倍にしても 7 倍程度。**解像度の効きは緩やか。**

### 1-3. プリミティブ数スケーリング（res 128 / 全球を同一座標に重ねて表面積を固定）

| prims | 1 | 2 | 4 | 8 | 16 | 32 | 64 | 128 | 256 |
|---|---|---|---|---|---|---|---|---|---|
| ms | 5.2 | 8.1 | 14.0 | 25.9 | 43.1 | 96.6 | 232.0 | 412.5 | 1088.9 |

**ほぼ線形。1プリミティブあたり約 4.2 ms（res 128）。** 表面積を固定した対照条件なので、
これは純粋に「ボクセルごとに全プリミティブを舐めるコスト」を表す。

### 結論
**支配項は解像度ではなくプリミティブ数。** 実運用の形状（216球・res 128）では 1 回 5〜7 秒に達する。

---

## 2. 「1本にまとめる」対「N本に分ける」

同一モデル（6×6×6=216球）を空間的に N 分割し、**同じ解像度・同じドメイン**で N 回評価した合計時間と、
1回でまとめて評価した時間の比較。

| res | 1本 | N=2 | N=4 | N=8 |
|---|---|---|---|---|
| 64 | 2268 ms | 1331 ms (0.59x) | 749 ms (0.33x) | 548 ms (0.24x) |
| 128 | 5053 ms | 2864 ms (0.57x) | 1904 ms (0.38x) | 1215 ms (0.24x) |
| 192 | 6138 ms | 5418 ms (0.88x) | 3634 ms (0.59x) | 2280 ms (0.37x) |

さらに、各ツリーが**自分の形状に合わせた狭いドメイン**を持つ場合（＝ Auto Domain が実際に選ぶ状態。
同じ res でも実効解像度は上がる）:

| res | 1本 | N=2 | N=4 | N=8 |
|---|---|---|---|---|
| 128 | 5191 ms | 3792 ms (0.73x) | 2551 ms (0.49x) | 1584 ms (0.31x) |
| 192 | 6241 ms | 6330 ms (1.01x) | 4456 ms (0.71x) | 2725 ms (0.44x) |

### 結論
**ツリー分割は性能上のコストではなく、性能上の利得。**
1本あたりのプリミティブ数が減ることと、疎ブロック検出で空領域が落ちることが効いている。
分割してなお実効解像度を上げても（狭ドメイン）、res 192 / N=2 の 1.01x を唯一の例外として、
すべて 1 本にまとめた場合以下のコストで済む。

---

## 3. ツリーが増えるだけで重くなるか（アイドル時のコスト）

depsgraph ハンドラ `_sdf_depsgraph_handler_impl` を、**何も変更していない状態**で 20 回呼んだ中央値。

| ツリー数 | 2 | 4 | 8 | 16 |
|---|---|---|---|---|
| ms/イベント | 0.025 | 0.118 | 0.307 | 0.744 |

参考:
- 状態フィンガープリント計算: スタック 27 個で 0.32 ms / 1 ツリー
- `update_sdf_mesh` のリクエスト発行まで（スタック→プリミティブ構築込み）: 1.17 ms

### 結論
本数に比例するが**16本で 0.74 ms/イベント**。1フレーム（16.7 ms）に対して十分小さい。
**ツリー数そのものを制限する性能上の根拠は無い。**

---

## 4. Rust ビルドコスト

| 条件 | 時間 |
|---|---|
| `src/lib.rs` のみ変更 → `cargo build --release` | **3.45 秒** |
| wgpu 依存まで再コンパイルが走った場合 | 30 秒 |

### 結論
**Rust のコンパイル時間はリリース判断の材料にならない。**
3プラットフォーム配布の実コストはコンパイルではなく、`cross_platform_build_notes.md` に
書かれている手順一式（`_MAC` / `_LINUX` ツリーの作成 34 ファイル、GitHub Actions、
二重ZIPの展開、セクション6の検証、macOS の Gatekeeper 対応）と、
**その工程で過去2回事故が起きている**（V16.1.1 の RUSTFLAGS 欠落、V16.1.3 の書き換え漏れ）という
リスクのほうにある。

---

## 5. バグの実証

### B1 確定（実証済み）
プロパティを変更したときに状態フィンガープリントが変わるかを直接比較した:

| プロパティ | 変更 | 結果 |
|---|---|---|
| `auto_domain` | True → False | **ハッシュが同一。更新が抑止される** |
| `domain_size` | 2.0 → 9.0 | OK |
| `resolution` | 48 → 64 | OK |
| `use_live_normals` | False → True | **ハッシュが同一** |

`auto_domain` は `update=update_sdf_callback` 付きなので `update_sdf_mesh` は呼ばれるが、
ハッシュが不変のため早期リターンする。**Auto Expand Domain のトグルは、他の変更を加えるまで効かない。**

### B2 確定（実証済み）
プリミティブ 8 個のツリーと 1 個のツリーを作って両方更新した結果:

```
tree0 (8 prims) verts = 2616
tree1 (1 prim)  verts = 2616
```

**1つの計算結果が全出力オブジェクトに適用されている。**

### B5 は「稀なレース」に格下げ（実測で再現せず）
GPU が計算中に 2 本目の要求を出した場合を実測したところ、
`update_sdf_mesh` 冒頭の `is_updating()` チェックが先に効き、`_pending_update = True` が立って
ハッシュは進まなかった。**当初「複数ツリーでは必ず踏む」と書いたが、これは誤り。**
損失が起きるのは冒頭チェックと実際の要求送出の間に他スレッドが GPU を掴んだ場合のみで、
狭いレースに留まる。

ただし **B3（ペンディング再試行がループ内 `return` で先頭の出力しか処理しない）と
`_pending_update` がグローバル1個であることは systematic な欠陥**であり、
複数ツリーでは 2 本目以降の予約更新が処理されない。ここは B5 とは別に確実に直す必要がある。

### 追加発見: V16.2.0 のコンソール版数表示が V16.1.3 のまま
```
rust_gpu_sdf_addon/__init__.py:100  print("SDF.R: Loader Marker V16.1.3 ...")
:182  print("SDF.R: --- Initializing GPU Engine (V16.1.3) ---")
:204  print("SDF.R: --- Starting GPU Warming-up (V16.1.3) ---")
```
`bl_info` は `(16, 2, 0)` で正しいが、コンソールマーカーが旧版数のまま。
通常版・`_MAC`・`_LINUX` の**3ツリーすべて**が同じ状態。
`cross_platform_build_notes.md` のチェックリストが「⚠️ V16.1.3 で漏れた」と警告している項目そのもので、
**V16.2.0 でも再発している**。サポート時にユーザーのログから版数を判別できないため、次のリリースで直したい。

---

## 6. 実測にもとづく設計判断への影響

### Phase 0 に Rust 変更は不要だった（計画の訂正）

当初「リクエストIDを Rust に持たせる」と計画したが、`GPU_CONTEXT` が単一 Mutex、
`IS_UPDATING` が単一フラグである以上、**同時に飛ぶ要求は常に高々1つ**である。
したがって Python 側が「いま投げた要求の持ち主」を1変数覚えておけば、
結果を正しい 1 体だけに適用できる。**`lib.rs` は無変更で B2 を修正できる。**

これにより Phase 0 は Python のみの変更となり、
- 3プラットフォームのバイナリ再ビルドが不要
- ABI 変更のリスクがゼロ
- Mac/Linux ツリーは Python がバイト単位で同一なので、そのまま同じ修正を反映できる

Rust への ID 導入が必要になるのは「複数の要求を同時に走らせたい」場合のみで、
GPU コンテキストが単一である限りその必要は生じない。
