# SDF.R (Rust-GPU-SDF) プロジェクト引継ぎ資料

## 1. プロジェクト概要
本プロジェクトは、Blender上で**「粘土を触るような直感的な造形体験」**を、Rust言語とGPU（WGPU/WGSL）の力で実現する次世代SDF（Signed Distance Field）モデリングアドオンです。
当初の物理シミュレーション開発から、現在は高度なSDFエンジンへと進化を遂げています。

### 現在のステータス (V14.1 / V15開発中)
- [x] **GPUスパース・アクセラレーション**: 必要な場所だけ計算するボクセル・ハッシング（Sparse Grid）により、広大なドメインと微細なディテールを両立。
- [x] **Dual Contouring & Marching Cubes**: 鋭いエッジと高速なプレビューの2つのアルゴリズムを搭載。
- [x] **ダイナミック・デフォームスタック**: ベンド、ツイスト、テーパー、ストレッチを任意の順序で重ね掛け可能。
- [x] **レイアウト・スタッキング**: ミラー、放射状、螺旋、グリッド、ジッターによる高度な複製配置。
- [x] **自己完結型バイナリ**: Rustビルド成果物をアドオン内に同梱し、特別な環境構築なしで動作。
- [x] **iGPU互換性**: Intel Iris Xe等のiGPU(FXCコンパイラ)向けにシェーダーを最適化（ループ手動展開によるコンパイルOOM回避）。

## 2. アーキテクチャ構成

### 🐍 Python層 (UI / データ管理)
- **`properties.py`**: デフォームスタック、レイアウト設定、出力品質の定義。
- **`engine.py`**: Rustモジュールへのデータ受け渡しと非同期メッシュ生成の制御。
- **`ui.py`**: Nパネル（SDF-Rタブ）のモダンなUI構成。

### 🦀 Rust層 (コアエンジン)
- **`src/lib.rs`**: Pythonからのエントリポイント。GPUバッファ管理、非同期計算スレッド、CPUフォールバック。
- **`src/gpu.rs`**: WGPUバックエンドの初期化とコンピュートパイプライン制御。

### 🔥 Shader層 (WGSL/GLSL)
- **`src/sdf.wgsl / dc.wgsl`**: GPU上でのメイン計算（MC/DC）。スパースグリッド制御。
- **`shader.py`**: ビューポート上での高速ゴーストプレビュー（レイマーチング）用GLSL。

## 3. 主要ファイル構成
- `Rust-GPU-SDF-V14_1/`: 現在の最新開発ディレクトリ。
- `agent-work-log.json`: 全開発工程の詳細なログ。
- `_Archive/`: 過去バージョン(V2〜V14)の遺産。

## 4. 開発・ビルド手順
1. Rustソース (`src/`) または Pythonソース (`rust_gpu_sdf_addon/`) を修正。
2. PowerShellで `.\build_sdf_addon.ps1` を実行（Rustビルドとアドオンへの配備を自動化）。
3. Blenderで `SDF-R` パネルの `Live Update` をONにして確認。
   - ※ iGPU環境でのコンパイルエラーに備え、WGSLのループは手動展開（Unrolled）されています。

---
作業お疲れ様でした。V14.1は「実戦投入可能な品質」に到達しています。
引き続き、驚異的な造形体験を追求していきましょう。
