# SDF.R V16.1.0 — SNS投稿用文面（和文）

X（Twitter）を主想定。そのままコピペ可。
※視覚系アドオンなので、**動画かGIFの添付が最優先**です。文面は添え物と割り切って短くしています。
　おすすめの絵：カーブを描く → そのままパイプになる → 他の形状と滑らかに繋がる、の一連。

---

## 1. メイン告知（これ1本でOK）

```
SDF.R V16.1.0 公開しました🌀

Blender標準のカーブで描いたパスが、そのままSDF形状になります。
他のプリミティブと同じようにブレンド・減算・交差ができます。

カーブは何本でも追加でき、1本ごとに太さ・演算・色を指定可能。

購入者様は無償アップデート
Windows / macOS / Linux 対応

#b3d #Blender
```

---

## 2. スレッド用（1の続きに連ねる）

**2-1／Curve Ref**
```
カーブの登録は2通り。

・SDFコレクションに入れる
・「Curve Ref」で、シーンのどこにあるカーブでも参照する

後者は元のカーブを一切動かさないので、アニメーションやGeometry Nodesと併用したままSDF形状にできます。
同じカーブを複数参照して、別々の太さ・色にすることも。
```

**2-2／その他の新機能**
```
他にも

📐 Bezier プリミティブ
　始点/終点の半径を個別指定できる2次ベジェ。テーパーのついた角や爪、触手などに

💎 Transmission
　生成マテリアルにガラスのような透過を設定できるように
```

**2-3／修正（既存ユーザー向け・重要）**
```
🔧 修正
Radial/Spiral配列 と Individual/Step Rotation を組み合わせた時、
また Radial Axis を X/Y にした時にジオメトリが欠けることがある問題を直しました。

細長い形状・非対称な形状すべてに影響します。
以前作った配列も、よければ再確認してみてください。
```

**2-4／アップデート時の注意（必須で入れたい）**
```
⚠️ アップデート前にお願い

今回GPUシェーダーが変わっています。
Blender終了後、下記を削除してから起動してください。

%APPDATA%\Blender Foundation\Blender\<バージョン>\datafiles\rust_gpu_sdf\shader_cache.bin

飛ばすと「Initializing...」で止まることがあります。
削除後の初回起動は15〜45秒ほどかかります。
```

---

## 3. 単発投稿（機能ひとつを推す・後日の再告知用）

**Curve Sync**
```
Blenderのカーブで線を描くと、そのままSDFのパイプになります。

メッシュを別に持つのではなく“SDFそのもの”になるので、
他の形状と滑らかに溶け合ったり、逆に削ったりできます。

SDF.R V16.1.0

#b3d #Blender
```

**Curve Ref**
```
カーブをSDFコレクションに移動したくない時は「Curve Ref」。

シーンのどこにあるカーブでも参照でき、元のオブジェクトは動かしも書き換えもしません。
同じカーブを複数の設定で使い回せます。

SDF.R V16.1.0

#b3d #Blender
```

**Bezier プリミティブ**
```
始点と終点で半径を変えられる2次ベジェのプリミティブを追加しました。
数値だけでテーパーのついた曲線が作れるので、角・爪・触手などに。

配列やデフォームとも普通に組み合わせられます。

SDF.R V16.1.0

#b3d #Blender
```

---

## 4. 長文が使える場合（Bluesky / Misskey / Mastodon / note 等）

```
SDF.R V16.1.0 を公開しました。

これまでSDF.Rの形状は、すべて数値で指定する必要がありました。
今回から、Blender標準のカーブツールでパスを描くと、それがそのままSDF形状になります。

生成されたパイプは他のプリミティブとまったく同じ扱いなので、
滑らかにブレンドさせたり、減算・交差で削ったりできます。
Bezier / Poly / NURBS、閉じたカーブにも対応しています。

登録方法は2通りあり、SDFコレクションに移動する方法のほか、
「Curve Ref」でシーン内の任意のカーブを参照することもできます。
参照方式なら元のカーブは動かさないので、アニメーションやGeometry Nodesと併用したまま使えます。

このほか、始点/終点の半径を個別指定できるBezierプリミティブ、
マテリアルのTransmission（透過）にも対応しました。

修正として、Radial/Spiral配列とIndividual/Step Rotationを組み合わせた際に
ジオメトリが欠けることがある問題を直しています。細長い形状すべてに影響するため、
以前作った配列も再確認いただくとよいかもしれません。

購入者の方は無償アップデートです。Windows / macOS / Linux 向けを用意しています。

⚠️ GPUシェーダーが変わっているため、アップデート前に
shader_cache.bin の削除をお願いします（詳細はリリースノートに記載しています）。

#b3d #Blender
```

---

## 5. ハッシュタグ候補

- 定番：`#b3d` `#Blender`
- 併用候補：`#BlenderAddon` `#Blender добавка` は使わず、`#3DCG` `#モデリング`
- 日本語圏向け：`#Blender勉強中` `#3DCG` を1〜2個まで（付けすぎると逆効果）

---

## 補足：文面で避けた表現

ビューポートのゴーストプレビューは、Curve Sync については**軽量なガイド線表示**です
（プレビューを軽く保つための仕様で、正確な形状は生成メッシュ側に出ます）。
そのため「リアルタイムでパイプが見える」といった表現は意図的に使っていません。
動画を撮る際も、確定メッシュ表示（Show Result Mesh ON）の状態を映すのがおすすめです。
