# SDF.R V16.1.3 — SNS投稿用文面（和文）

X（Twitter）を主想定。そのままコピペ可。文字数制限は考慮していません。

> **V16.1.0 のときとの違い:** 今回は機能追加ではなく**不具合修正**のリリースです。
> 派手さはありませんが、「プレビューには出るのにメッシュに出ない」という症状は
> 経験した人に刺さります。**Before / After の比較映像が最も効きます。**
>
> おすすめの絵：箱に球で切り欠きを入れ、Symmetry X をオン。切り欠きを対称面の**負側**に置いた
> 状態で、V16.1.2 では切り欠きの無い箱が出て、V16.1.3 では正しく両側に切り欠きが入る、という
> 並べ方。**プレビュー画面には両方とも切り欠きが見えている**ことが分かる画作りにすると、
> 症状の説明が一枚で済みます。

---

## 1. メイン告知（これ1本でOK）

```
SDF.R V16.1.3 公開しました🔧

全体対称化（Symmetry X/Y/Z）で、対称面の負側に置いた形状がメッシュに出ない不具合を修正しました。

プレビューには出ているのに、生成すると消える。
特にブーリアンだと、土台はきれいに出て切り欠きだけが消えるので気づきにくい症状でした。

原因はこちらのコードです。ご報告くださった方に感謝します。

購入者様は無償アップデート
Windows / macOS / Linux

#b3d #Blender
```

---

## 2. スレッド用（1の続きに連ねる）

**2-1／何が起きていたか**

```
SDF.R には、プレビューとメッシュ生成という2つの実装があります。

プレビューはSDFを直接レイマーチングする。
メッシュ生成は「空間のどこを見るか」を先に決めて、その範囲だけをGPUでサンプリングする。

対称化は前者では正しく、後者では間違っていました。しかも2箇所同時に。
```

**2-2／2つの間違い**

```
① 探索範囲が対称面の片側しか覆っていなかった
　「−max〜+max」であるべきバウンディングボックスを「0〜+max」に切り詰めていました。
　負側にあるものは、メッシュ化が始まる前に捨てられていた。

② プリミティブ中心を対称側へ折り返していなかった
　プレビュー用シェーダーはやっている処理を、メッシュ生成用シェーダーがやっていなかった。

この2つが噛み合うと、探索範囲の中身が空になります。
```

**2-3／ブーリアンだと気づきにくい理由**

```
単体の形状が負側にあると、メッシュが空になるので「何かおかしい」と分かります。

ブーリアンだと違います。
土台の形状は正常にメッシュ化され、切り欠きだけが消える。
エラーも出ず、プレビューには切り欠きが表示され続ける。

箱を球で減算した実測値（Symmetry X）:
・切り欠きを X=+2 に置く → 正常
・切り欠きを X=−2 に置く → 切り欠きの無い箱がそのまま出力

修正後はどちらも同じ結果になります。
```

**2-4／影響範囲（ここは正確に）**

```
常に壊れていたわけではありません。

影響したのは「プリミティブの中心が対称軸の負側にある」場合だけです。
正側・対称面上にあるものは正常でした。

やっかいなのは、正側に対になる形状があると結果が正しく見えてしまうこと。
対称化を使う典型的なシーンほど、不具合が隠れやすい状態でした。

Marching Cubes / Dual Contouring どちらも修正済みです。
```

**2-5／もうひとつの修正**

```
🖱️ The Stack のレイヤー選択

これまではレイヤーの「名前」をクリックしたときだけ選択が連動していました。
行の他の場所を押すとハイライトだけ動いて、ビューポートの選択は変わらない状態。

行のどこをクリックしても選択されるようにしました。
ビューポート側で複数選択したものが1つに減ることもありません。

こちらもご報告からの修正です。
```

**2-6／アップデートについて**

```
✅ 事前の作業は不要です

V16.1.0 のときはシェーダーキャッシュの削除をお願いしましたが、今回は不要です。
起動に失敗した場合、アドオンが自分でキャッシュを消して再試行するようになっています。

そのままインストールしてください。
```

**2-7／macOS / Linux について**

```
🖥️ macOS / Linux 版

「問題なく動いています」というご連絡を複数の方からいただきました。ありがとうございます。
動作確認が取れたのは、実はこれが初めてです。

私は Windows で開発していて Mac も Linux の実機も持っていないため、
これらのプラットフォームは皆さんが教えてくださる分だけ良くなります。

今回からアドオンのコードは3プラットフォームでバイト単位で同一になりました。
macOS / Linux 版は、私が手元でテストできるコードそのものを動かしています。
```

---

## 3. 単発投稿（後日の再告知用）

**症状ベース（検索で拾われることを狙う）**

```
SDF.R で「Symmetry をオンにしたのにメッシュに反映されない」「ブーリアンの切り欠きがプレビューには
出るのに生成メッシュに出ない」という症状に心当たりのある方へ。

V16.1.3 で修正しました。設定の誤りではありません、こちらのバグでした。

対称面の負側に中心があるプリミティブが、メッシュ生成の前に捨てられていました。

#b3d #Blender
```

**開発姿勢として**

```
不具合の報告をいただいたとき、まず自分のコードを疑うようにしています。

今回の対称化の件も、報告者の方の環境ではなく私のミスでした。
しかも「正側に形状を置いていれば遭遇しない」種類のもので、
自分のテストでは踏めないバグでした。

外からの報告がなければ見つかりませんでした。感謝しています。

SDF.R V16.1.3

#b3d #Blender
```

**スタック操作**

```
SDF.R の The Stack、レイヤーの行はどこをクリックしても選択できるようになりました。

これまでは名前の部分だけが反応していて、
「選んだつもりなのにビューポートの選択が変わらない」状態でした。

こういう細かいところのご指摘、本当に助かります。

SDF.R V16.1.3

#b3d #Blender
```

---

## 4. 長文が使える場合（Bluesky / Misskey / Mastodon / note 等）

```
SDF.R V16.1.3 を公開しました。不具合修正のリリースです。

全体対称化（Symmetry X/Y/Z）を有効にしたとき、対称面の負側に中心があるプリミティブが
生成メッシュから消える不具合を修正しました。ビューポートのプレビューには正しく表示されるため、
「プレビューには出るのに、メッシュにならない」という分かりにくい症状になっていました。

特にブーリアンと組み合わせた場合が厄介でした。単体の形状なら空のメッシュになって異常だと
気づけますが、減算に使った形状が負側にあると、土台はきれいにメッシュ化されて切り欠きだけが
消えます。エラーも出ず、プレビューには切り欠きが表示され続けます。

原因は、プレビューとメッシュ生成が別々の実装であることに起因していました。メッシュ生成は
サンプリング前に「空間のどこを見るか」を決めますが、対称軸が有効なときにその範囲を正側だけに
切り詰めており、さらにメッシュ生成用シェーダーがプレビュー用シェーダーと違って
プリミティブ中心を対称側へ折り返していませんでした。この2つが重なると、探索範囲の中身が
空になります。

影響範囲は正確にお伝えしておきます。常に壊れていたわけではなく、プリミティブの中心が
対称軸の負側にある場合だけです。正側や対称面上にあるものは正常にメッシュ化されていました。
ただ、正側に対になる形状があると結果が正しく見えてしまうため、対称化を使う典型的なシーンほど
気づきにくい状態でした。Marching Cubes / Dual Contouring の両方で修正済みです。

あわせて、The Stack のレイヤーが行のどこをクリックしても選択できるようになりました。
これまでは名前の部分だけが反応していました。

どちらも購入者の方からのご報告によるものです。ありがとうございました。

購入者の方は無償アップデートです。Windows / macOS / Linux 向けを用意しています。
今回はシェーダーキャッシュの削除は不要です。

#b3d #Blender
```

---

## 5. ハッシュタグ

- 定番：`#b3d` `#Blender`
- 併用候補：`#3DCG` `#モデリング`（1〜2個まで）
- 今回は不具合修正なので、`#新機能` 系のタグは付けない

---

## 補足：文面で避けた表現

**「Symmetry が動かなかった」と書かない。** 実際には正側・対称面上の形状は正常でした。
広く書くと「自分のところは動いていたのに」という混乱を招きますし、事実として不正確です。
**「負側に中心があるプリミティブ」**という条件を必ず添えてください。

**「ブーリアンのバグ」と書かない。** 原因はブーリアン処理ではなく対称化とメッシュ生成側です。
ブーリアンは症状が見えやすくなる条件にすぎません。ただし報告者の方の表現でもあり、
同じ症状で検索する人が使う言葉でもあるため、**症状の説明としては併記する**のが有効です。

**シェーダーキャッシュ削除を書かない。** V16.1.0 のときの文面を流用すると入りがちですが、
今回は不要です（起動失敗時にアドオンが自動で削除・再試行します）。不要な作業を案内すると
「面倒なアップデート」という印象だけが残ります。

**macOS / Linux を「テスト版」と書かない。** V16.1.2 でその表記は取りやめています。
