# SDF.R V16.2.1 — SNS投稿用文面（和文）

X（Twitter）を主想定。そのままコピペ可。文字数制限は考慮していません。

> **V16.2.0 のときとの違い:** 前回は「既存機能が名前どおりに動くようになった」リリースで、
> 嬉しさを伝えるのが難しい部類でした。**今回は逆に、伝えるのが簡単なリリースです。**
> できなかったことが、できるようになった。それだけです。
>
> しかも V16.2.0 の告知では **「別オブジェクトになる」と書いてはいけない**と自分で釘を刺して
> いました（当時は本当にできなかったため）。**今回はそれが書けます。** 前回の告知を見た人には、
> そこが一番効きます。
>
> **絵で解決してください。** おすすめは、**アウトライナーに結果メッシュが2つ並んでいる画**。
> 「別オブジェクトです」を一枚で証明できます。動かすなら、片方をドラッグして離す短い動画。
> 目・まぶた・本体のような**具体的な用途が分かる題材**だとなお良いです。
>
> Mirror Blend は**継ぎ目の寄り**で。0 と 0.3 を往復させれば、折れ目が消えるのが一目で分かります。

---

## 1. メイン告知（これ1本でOK）

```
SDF.R V16.2.1 公開しました🌳

「目を、本体とは別のオブジェクトとして出したい」
今までお答えできていなかったやつです。できるようになりました。

1つのシーンに SDF ツリーを何本でも。
それぞれが自分のパーツと結果メッシュを持つので、
最初から別オブジェクトです。分割作業は要りません。

購入者様は無償アップデート
Windows / macOS / Linux

#b3d #Blender
```

---

## 2. スレッド用（1の続きに連ねる）

```
2/
パネルの一番上に Tree 行が出ます。
ドロップダウンで編集対象を選び、+ で追加。
ビューポートでパーツをクリックするだけでも、そのツリーに切り替わります。

Finalize は選択中のツリーだけに効くので、
1本焼いても残りは編集可能なままです。
```

```
3/
分けると遅くなりそうですが、逆でした。

216球・解像度128で
1本 5053ms → 4本 1904ms → 8本 1215ms

1リクエストあたりの個数が減って、
疎ブロック検出も効きやすくなるためです。
ツリー数に上限はありません。
```

```
4/
はめ合いのための Tree Reference も入れました。

あるツリーから別のツリーの形を読んで、
Blend（足す）か Subtract（削る）。

瓶から蓋のくぼみを削り出せば、両者はぴったり合います。
蓋を編集すると、くぼみも追従します。
```

```
5/
もう1つ、ミラーの継ぎ目を丸められるようになりました🪞

プリミティブの Mirror は空間を折り返す仕組みなので、
Smoothness をいくら上げても真ん中の折れ目には効きません。
折り畳まれた空間には形が1つしかなく、ブレンドする相手がいないためです。

実測で最大84°。陰影ではなく、形そのものの折れ目でした。
```

```
6/
Mirror Blend は両側を別々に評価して結合します。

折れ目 44本/84.1° → 0本/8.5°
実体の球を2つ置いて Smooth Union したものと、頂点単位で一致します。

既定は 0（従来どおり）なので、
既存ファイルの見た目は変わりません。
```

---

## 3. 単発投稿（後日の再告知用）

```
SDF.R、1つのシーンで SDF ツリーを何本でも持てるようになりました。

各ツリーが自分の結果メッシュを持つので、
パーツは最初から別オブジェクトです。

同じツリーの中では重なった形は1つの立体になりますが、
別のツリー同士はそもそも干渉しません。

#b3d #Blender
```

```
SDF.R のミラー、真ん中の折れ目が取れるようになりました。

Mirror は空間の折り返しなので、Smoothness が効かない場所でした。
Mirror Blend で両側を別々に評価します。

最大84° → 8.5°
実体2つを Smooth Union したのと同じ結果です。

#b3d #Blender
```

```
SDF.R の Tree Reference、はめ合いに使えます。

瓶のツリーから蓋のツリーを Subtract すると、
蓋の形にくぼみが空きます。
蓋を編集すれば、くぼみも追従します。

#b3d #Blender
```

---

## 3-B. V16.2.0 をお使いの方への案内（優先度高）

V16.2.0 には、公開後に見つかった不具合が1件あります。**気づいていない方がいる可能性が高い**ため、
機能の告知とは別に、これ単体で1本出しておくことをおすすめします。

不具合の性質上、**利用者からは「エラー」ではなく「なんとなくゴーストが出なくなった」としか
見えません**。Windows でデスクトップから起動しているとコンソールも見えないので、
不具合として報告される見込みが薄いぶん、こちらから声をかける価値があります。

```
SDF.R V16.2.1 を公開しました。

V16.2.0 に、1本のツリーの要素が16個を超えると
ゴーストプレビューが出なくなる不具合が入っていました。
V16.2.1 で修正しています。

メッシュ生成には影響しません。
保存したファイルも壊れていません。
止まるのはリアルタイムのプレビュー表示だけです。

数えるのはレイアウト展開後の要素数なので、
仕切りで配列を効かせていると、置いた部品が少なくても届きます。

心当たりのある方は更新をお願いします。

#b3d #Blender
```

---

## 4. 長文が使える場合（Bluesky / Misskey / Mastodon / note 等）

```
SDF.R V16.2.1 を公開しました。

これまで SDF.R のシーンが持てるツリーは1本だけで、出力されるメッシュも1つでした。
モデルのパーツを別オブジェクトにしたいというご要望に、これまで何度か
「SDF 出力をもう1つ作ってください」とお答えしてきたのですが、
アドオンの中からそれを行う手段自体がありませんでした。

今回はその手段そのものです。

1つのシーンに SDF ツリーを何本でも置けます。それぞれが自分のパーツ用コレクション、
自分の設定、自分の結果メッシュを持ちます。作った時点で別オブジェクトなので、
後から分割する作業はありません。

同じツリーの中では、重なった形は必ず1つの繋がった立体になります。
Layer Boundary はブレンドを止めますが、合流自体は Union のままです。
別のツリー同士は、そもそも一切干渉しません。

分けると重くなりそうに思えますが、実測では逆でした。
216球・解像度128のモデルで、1本 5053ms、4本に分けて 1904ms、8本で 1215ms。
1リクエストあたりのプリミティブ数が減るためです。上限は設けていません。

はめ合いを作るための Tree Reference も入れました。
あるツリーから別のツリーを Blend または Subtract で取り込めます。
瓶から蓋のくぼみを削り出すような使い方です。

もう1つ、プリミティブのミラーの継ぎ目を丸める Mirror Blend を追加しました。
Mirror は空間を折り返す仕組みなので、Smoothness は真ん中の継ぎ目に効きません。
折り畳まれた空間の中には形が1つしかなく、ブレンドする相手が存在しないためです。
実測で最大84°の、陰影ではなく形そのものの折れ目でした。
両側を別々に評価するようにして、0本/8.5°になりました。
既定は0なので、既存ファイルの見た目は変わりません。

購入者様は無償アップデートです。Windows / macOS / Linux。

なお、GPU シェーダーのコードが変わったため、更新後の初回起動だけ
シェーダーの再構築が入ります。内蔵GPUだと数分かかることがあります。
コンソールに GPU Engine Ready! と出れば完了です。

#b3d #Blender
```

---

## 5. ハッシュタグ

`#b3d` `#Blender` `#3DCG` `#SDF` `#モデリング`
造形寄りの絵なら `#3Dprinting` `#3Dプリンター` も。

---

## 補足：文面で避けた表現

**「自動で分割される」と書かない。** 分割機能ではありません。ツリーは**作るもの**であって、
既存の1本を割る機能はありません。「パーツごとにツリーを作る」という言い方をしてください。
ここを曖昧にすると、既存ファイルを開いて自動で分かれることを期待されます。

**「全部のツリーがプレビューに出る」と書かない。** ゴーストプレビューは**アクティブなツリー1本
だけ**です。ただし**結果メッシュは全ツリーぶん常に見えています**ので、「見えない」という誤解も
与えないでください。制限があるのはライブプレビューだけ、と正確に。

**Subtract を「常に厳密」と書かない。** 参照先が Union だけで構成されていれば厳密ですが、内部で
Subtract / Intersect を使っている場合は近似です。パネルにも注意が出ます。Blend は厳密です。

**「Mirror が壊れていた」と書かない。** Mirror の複製自体は正しく動いていました。効かなかったのは
**継ぎ目に Smoothness が効かない**という点で、これは折り返しという仕組み上そうなるものです。
「今まで丸められなかった場所が丸められるようになった」という言い方をしてください。

**Mirror Blend で既存の見た目が変わると誤解される書き方をしない。** 既定は 0 で、そのときは従来と
完全に同じ経路を通ります。**プリミティブの Smoothness（既定0.2）とその継承も無変更**です。
V16.2.0 のときと同様、この2つは必ず別の名前で呼んでください。

**Radial / Grid も直ったと書かない。** 直っていません。同じ折れ目が出ます。ミラーだけです。

**プレビューの不具合を「メッシュが壊れる」と書かない。** 壊れません。メッシュ生成は完全に別経路で、
出力メッシュも保存済みの .blend も正しいままです。止まるのは**リアルタイムのゴースト表示だけ**で、
更新すれば元に戻ります。ここを曖昧にすると、無事なファイルを作り直す人が出ます。

**「プレビューが壊れていた」と一般化して書かない。** 条件は**1本のツリーの要素が17個以上**のときで、
16個までは正常に動いていました。条件を書かずに広げると、影響のない方まで不安にさせます。

**要素数を「置いた部品の数」と書かない。** 数えるのは**レイアウト展開後**の数です。仕切りの Grid や
Radial は要素を複製するので、部品3個でも 4×4 の配列を効かせれば18要素になります。
「部品17個以上」と書くと、少ない部品で踏んだ方が自分は該当しないと判断してしまいます。

**シェーダーキャッシュ削除を書かない。** 手動削除は不要です。ただし**更新後の初回起動が長い**点は
書いてください。内蔵GPUで数分かかることがあり、黙っているとフリーズと誤解されます。
（Intel Iris Xe で194秒を実測。2回目以降は1〜2秒）

---

## 参考：V16.2.0 の告知との関係

V16.2.0 の SNS 文面では、**「分離する」「別オブジェクトになる」は不可**と明記していました。
当時は Layer Boundary が「溶けなくなる」だけで、実際には1つの繋がった立体のままだったためです。

**その制約が今回外れました。** 前回の告知を見ている人ほど効く差分なので、
「前回は書けなかったことが書けるようになった」という文脈で触れるのは有効です。
ただし **Layer Boundary が別オブジェクトを作るようになったわけではない**点は変わりません。
別オブジェクトになるのは**ツリーを分けたとき**です。混ぜないでください。
