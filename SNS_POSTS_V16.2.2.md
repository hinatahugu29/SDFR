# SDF.R V16.2.2 — SNS投稿用文面（和文）

X（Twitter）を主想定。そのままコピペ可。文字数制限は考慮していません。

> **V16.2.1 のときとの違い:** 前回は「できなかったことができるようになった」大型リリースで、
> 伝えるのが簡単な部類でした。**今回は修正1件のパッチリリースです。** 新機能はありません。
>
> **全員に向けて出す話ではありません。** 対象は V16.2.1 で Mirror Blend を試した人だけで、
> その人たちは高い確率で「この機能は壊れている」と結論して使うのをやめています。**告知の目的は
> 宣伝ではなく、その誤解を解くこと**です。使っていない人には見える変化がありません。
>
> **絵で解決してください。** おすすめは、**Mirror Blend を 0 → 1 へ上げる短い動画を、
> V16.2.1 と V16.2.2 で並べたもの**。旧版は細くなって消え、新版は膨らむ。並べれば一目で伝わります。
> 1枚で済ませるなら、**プレビューとメッシュを同時に写した比較画像**です。
>
> **「不具合でした」を隠さないでください。** 隠すと、旧版で見た挙動が正しかったのか
> 分からないままになります。同時に、**メッシュは無事**であることを必ず併記してください。

---

## 1. メイン告知（これ1本でOK）

```
SDF.R V16.2.2 公開しました🪞

V16.2.1 の Mirror Blend で
「上げると細くなる」「上げ切ると消える」
という挙動を見た方へ。

あれはプレビュー側の不具合でした。
機能自体は正しく動いていて、
生成されるメッシュは最初から正常です。

今回の更新でプレビューが最終メッシュと
同じ向きに膨らむようになりました。

購入者様は無償アップデート
Windows / macOS / Linux

#b3d #Blender
```

---

## 2. スレッド用（1の続きに連ねる）

```
2/
なぜ逆になっていたか。

最終メッシュはミラーした両側を別々に評価して
Smooth Union で繋ぐので、継ぎ目付近で外へ膨らみます。

プレビューは1形状を1回しか評価できないため
同じことができず、代わりに折り返し自体を
鈍らせる近似を使っていました。

これは内側へ丸める処理です。向きが逆でした。
```

```
3/
値が大きいと、内側への食い込みが形を飲み込みます。

半径0.15・オフセット0.25 の球で、
Blend 0.8 でプレビューから形が消えました。
実測と式が一致しています。

メッシュ側は同じ条件で正常に出ていたので、
プレビューとメッシュの食い違いが
値を上げるほど広がる状態でした。
```

```
4/
修正後は、折り返しは abs() のまま
距離側に補正を入れる方式にしました。

通常使う範囲でメッシュとの差は 0.02〜0.12 程度。
（従来は 0.11、そこから先は消滅）

近似であることは変わらないので、
丸まり方まで一致するわけではありません。
向きと量が揃った、という修正です。
```

```
5/
もう1点、パネルに警告を追加しました。

Blend を Offset の2倍より大きくすると、
継ぎ目だけでなく形全体が太ります。

両側を滑らかに繋ぐ以上、避けられない挙動です。
紛らわしいのは、ミラー軸と直交する方向まで
太るところ。

太らせずに継ぎ目だけ丸めたいときは、
Blend ではなく Offset を上げてください。
```

---

## 3. 単発投稿（後日の再告知用）

```
SDF.R の Mirror Blend、
プレビューが最終メッシュと同じ向きに動くようになりました。

V16.2.1 では逆向きに丸めていて、
値を上げるとプレビューから形が消えていました。
メッシュ側は正常だったので、表示だけの問題です。

#b3d #Blender
```

```
SDF.R の Mirror Blend を使うときの注意点。

Blend を Offset の2倍より大きくすると、
継ぎ目だけでなく形全体が太ります。
両側を滑らかに繋ぐ以上、避けられない挙動です。

V16.2.2 からはパネルが警告を出します。
太らせたくないときは Offset の方を上げてください。

#b3d #Blender
```

---

## 4. 長文が使える場合（Bluesky / Misskey / Mastodon / note 等）

```
SDF.R V16.2.2 を公開しました。修正1件の小さな更新です。

V16.2.1 で追加した Mirror Blend は、ミラーした形の中央に残る折れ目を丸める機能です。
これを試したときに、値を上げるとビューポートの形が細くなる、あるいは上げ切ると
形が消える、という挙動をご覧になった方がいらっしゃると思います。
プレビュー側の不具合でした。機能自体は正しく動いていました。

生成されるメッシュは最初から正しい状態です。メッシュ生成はプレビューとは完全に別の
経路で動いていて、プレビュー用のコードに触れていません。V16.2.1 で作ったメッシュも、
保存したファイルも無事です。

原因を書いておきます。最終メッシュは両側を別々に評価して Smooth Union で繋ぐので、
継ぎ目の付近で表面が外へ膨らみます。プレビューは1フレームにつき1形状を1回しか
評価できないので同じことができず、折り返しそのものを鈍らせる近似を使っていました。
これは内側へ丸める処理で、向きが逆でした。値が大きいと形を完全に飲み込みます。

今回、折り返しは元の abs() に戻し、距離の側に補正を入れる方式に変えました。
通常使う範囲でメッシュとの差は 0.02〜0.12 程度です。近似であることは変わらないので、
丸まり方まで一致するわけではありません。

あわせて、Blend を Offset の2倍より大きくすると継ぎ目だけでなく形全体が太る点について、
パネルに警告を出すようにしました。これは不具合ではなく、両側を滑らかに繋ぐ以上
避けられない挙動です。太らせずに継ぎ目だけ丸めたい場合は Offset を上げてください。

Mirror Blend を使っていない場合、見える変化はありません。値が0のときは従来と
まったく同じ経路を通ります。

購入者様は無償アップデートです。Windows / macOS / Linux。

なお、GPU シェーダーのコードが変わったため、更新後の初回起動だけ
シェーダーの再構築が入ります。内蔵GPUだと数分かかることがあります。
コンソールに GPU Engine Ready! と出れば完了です。

#b3d #Blender
```

---

## 5. ハッシュタグ

`#b3d` `#Blender` `#3DCG` `#SDF` `#モデリング`

---

## 補足：文面で避けた表現

**「メッシュが壊れていた」と書かない。** 壊れていません。メッシュ生成は完全に別経路で、出力メッシュも
保存済みの .blend も正しいままです。誤っていたのは**リアルタイムのゴースト表示だけ**です。ここを
曖昧にすると、無事なファイルを作り直す人が出ます。

**「Mirror Blend が壊れていた」と書かない。** 機能は正しく動いていました。誤っていたのは**その
プレビュー表示**です。ただし**利用者から見れば機能が壊れて見えていた**のも事実なので、「壊れて
見えたと思いますが、原因はプレビューでした」という順で書いてください。機能側に非がなかったことを
先に主張すると、言い訳に読まれます。

**「プレビューが最終メッシュと一致するようになった」と書かない。** 近似のままです。揃ったのは
**向きと、おおよその量**です。実測で 0.02〜0.12、2軸・3軸の Blend 2.0 で 0.38 ほどずれます。
「同じ向きに動くようになった」までにとどめてください。

**全員に更新を急かさない。** Mirror Blend を使っていない方には見える変化がありません。
V16.2.0 のプレビュー不具合（要素17個以上で表示が止まる）のときとは性質が違います。あちらは
気づけない不具合でしたが、今回は**該当する人は既に気づいていて、誤解している**状態です。

**Radial / Grid も直ったと書かない。** 今回の修正はミラーのプレビューだけです。Radial と Grid には
引き続き折れ目が出ます（仕組み上の制限で、V16.2.1 から変わっていません）。

**Blend を上げると太る件を「修正した」と書かない。** 修正していません。仕様です。追加したのは
**警告表示**だけです。「直った」と書くと、太らなくなったと誤解されます。

**初回起動が長いと書かない。** メッシュ生成エンジンは無変更なので、シェーダーキャッシュの再構築は
走りません。初回起動は通常どおりです。V16.2.1 の下書きから引き写した記述が V16.2.2 の文面に
残っていたもので、実機で 0.58 秒・キャッシュサイズ不変を確認済みです。手動削除も不要です。
