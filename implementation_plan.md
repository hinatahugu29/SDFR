# SeamlessCAD 次世代最適化プラン（バランス重視案）

現在のV7アーキテクチャ（Rustによる `cad_server.exe` の完全別プロセス化 ＋ ピクセルストリームオーバーレイ）は、商用ソフトとしてすでに驚異的な設計レベルに達しています。

OSネイティブのメモリ共有（DXGI）や完全なGPU CSGエンジンをゼロから作る「究極の限界突破」は開発工数が数ヶ月〜年単位に及ぶため、現在の貴重な実装時間を考慮し、**「いまあるV7の強みを最大限に活かし、最小の労力で最大の軽量化を得る（いい塩梅の）バランス案」**を提示します。

---

## 1. ピクセルストリームの「動的解像度ダウンスケール」（帯域幅の劇的削減）
TCPソケット経由での画像転送（特に4K環境）は、帯域幅とPythonのメモリコピーで首を絞めます。これを数行のコード変更で解決します。

*   **実装案**: ユーザーがマウスをドラッグしている間（`fast_mode = True`）のみ、Rust側での `render_viewport` の解像度を**縦横半分（面積で1/4）**に落とします。
*   **効果**: 毎秒2GBの転送量が500MBに激減し、一瞬でFPSが跳ね上がります。Blender側（`drawing.py`）では送られてきた小さな画像をそのまま全画面に引き延ばして描画するだけなので、実装コストはほぼゼロです。（マウスを離すとフル解像度に戻るため、CADとしての精密な見た目は損なわれません）。

## 2. 操作中の「ワイヤーフレーム（エッジ）抽出」の完全スキップ
現在、モデリング中に「重い」と感じる最大の理由は、SDFのピクセル描画ではなく、それに付随する**OpenCASCADEのエッジ頂点群の計算とJSON転送**です。

*   **実装案**: マウスドラッグ中（編集中）は、**面（ピクセルストリーム）だけ**をTCPで受け取って更新し、エッジ（シアン色の線など）の座標計算・転送リクエストは完全に停止（あるいは前回の状態のまま静止）させます。
*   **効果**: 最も重いCSGからの境界線抽出（B-Rep計算）がスキップされるため、Rust側の計算負荷が激減し、純粋なSDF描画の速さだけがユーザー体験として残ります。

## 3. レシピ（通信データ）のインスタンシング圧縮
同じボルトや穴を複数配置する際、現在はそれぞれが独立した形状として処理・転送されている可能性があります。

*   **実装案**: `core_bridge.py` からRustへ送信するレシピのJSON構造を少し最適化し、`[ベース形状のUUID, 変換行列配列[Matrix1, Matrix2...]]` という形で送るようにします。
*   **効果**: TCPソケットで送るデータサイズが減るだけでなく、Rust側のOpenCASCADEが「1個の形状」を計算するだけで済むようになり、ブーリアン演算の重さが劇的に改善されます。

---

## 結論と評価
上記3点は、OS固有のグラフィックスAPI（Vulkan/DirectX）を叩いたり、複雑なシェーダー言語を新規に書いたりする必要がありません。
**Python側のフラグ制御と、Rust側の軽微な解像度・通信プロトコル調整**だけで完結するため、実装時間が取れない現状において「最も投資対効果（コスパ）が高い」バランス案となります。

### User Review Required
この「バランス重視」の方向性（解像度ダウンスケール ＋ エッジ計算の遅延）は、現在のV7の枠組みの中で進める現実的な一歩としていかがでしょうか？
時間を要する機能実装は一旦保留とし、将来的に時間が取れた際へのロードマップとして本ドキュメントを保存しておきます。
